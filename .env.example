# Copy these values into .env and replace them with your real PostgreSQL settings.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=railway_hello_app
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL_MODE=Disable

# Optional single-line format
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/railway_hello_app
